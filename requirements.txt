fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9
PyYAML>=6.0
anthropic>=0.34        # 仅真实模式需要；MOCK 演示可不装
PyMuPDF>=1.24          # PDF 转图片（视觉识别 PDF 必需）
Pillow>=10.0           # 图像预处理（锐化、压缩、格式转换）
reportlab>=4.0         # PDF 方案导出
